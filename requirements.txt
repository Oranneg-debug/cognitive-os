openai>=1.0.0
psutil>=5.9.0
requests>=2.31.0
python-dotenv>=1.0.0
google-genai>=0.1.0
PyPDF2>=3.0.0

# Telegram Enhancements
youtube-transcript-api>=0.6.1

fastapi>=0.100.0
uvicorn>=0.23.0
python-telegram-bot>=20.3

# Inference backend: llama-swap + llama-server
# No Python SDK needed — llama-swap is an external process with an
# OpenAI-compatible API. The openai>=1.0.0 dependency (above) handles
# all inference calls. llama-swap manages model lifecycle automatically.
#
# Migration note (v3.0.0): The lmstudio==1.5.0 SDK dependency was removed.
# Model loading, unloading, and configuration is now handled declaratively
# via llama-swap's YAML config (config/llama-swap.yaml).

# GPU monitoring (Linux native — replaces pynvml on Windows/WSL2)
pynvml>=12.0.0

# JSON-schema validation for incoming load configs (Pydantic 2.x already
# installed transitively on the dev machine; declared here so fresh installs
# pick it up explicitly).
pydantic>=2.0

# YAML parsing for config files
ruamel.yaml>=0.18.0
pyyaml>=6.0