# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# Local data and outputs
mock_vault/
memory/
*.json

# Test fixtures MUST be tracked even though *.json is globally ignored.
# These are golden outputs for the Phase 2 routing regression suite (E2).
!tests/routing/*.json
!tests/**/golden_*.json
!tests/**/fixture_*.json

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs and scratch
*.log
scratch/

# Runtime ephemera — not source (A4: Governance Foundation hardening)
alpha_output.txt
beta_output.txt
*_output.txt
dev/failed_routings/*.failed.md
dev/failed_routings/*.sidecar.json
dev/.uow_log/
dev/.uow_staging/