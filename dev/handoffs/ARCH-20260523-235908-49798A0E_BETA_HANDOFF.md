---
proposal_id: ARCH-20260523-235908-49798A0E
phase: beta
status: in_progress
created: 2026-05-24 01:01:02
last_updated: 2026-05-24
handoff_type: beta_testing
related_proposal: "[[ARCH-20260523-235908-49798A0E_PROPOSAL]]"
kanban_card_id: "ARCH-2026052323590849798A0E"
source_note: ""
next_phase: Alpha Polish
tasks_completed: 0
tasks_total: 32
tasks_logical_total: 6
vault_kanban: "1. P - Seedlings/Dev-KanBan.md"
sections:
  - A — Core wiring (5 services)
  - B — Boot-time validation
  - C — Migration safety
  - D — Integration tests
  - E — Smoke test
  - F — Acceptance gate
---

# 🧪 Beta Testing Handoff — ARCH-20260523-235908-49798A0E

> **Generated**: 2026-05-24 01:01:02  
> **Proposal**: [[ARCH-20260523-235908-49798A0E_PROPOSAL]]  
> **Phase**: Beta Testing  
> **Status**: 🔧 In Progress — take this document to VS Code

---

## 🤖 Agent Context

> *This block is for AI agents (Cline/Claude in VS Code). It is not displayed in Obsidian reading mode.*

When a user references this handoff (e.g. *"work on ARCH-20260523-235908-49798A0E"*):

1. **Find the proposal** → `cognitive-os/dev/proposals/ARCH-20260523-235908-49798A0E_PROPOSAL.md`
2. **Work through the tasks** in `## 🔧 Implementation Tasks` below, ticking each `- [ ]` to `- [x]` as completed
3. **When all tasks are ticked** → update this file's frontmatter: `status: complete`, `tasks_completed: <n>`
4. **Update the proposal** → change `## 🧪 Beta Testing` status line to `✅ Complete`
5. **Update the Kanban card** at `vault_kanban` above → change `  - status: 🔍 Review` to `  - status: ✅ Ready for Alpha Polish`
6. **Tell the user** to drag the card to the `Alpha Polish` column to trigger the next council automatically

Backlinks to maintain:
- Proposal: [[ARCH-20260523-235908-49798A0E_PROPOSAL]]
- Source note: see `source_note` in frontmatter above (if set)
- Kanban card ID: see `kanban_card_id` in frontmatter above

---

## 📋 Executive Summary

```markdown
# **ARCH-20260523-235908-49798A0E: Governance Stack Integration Proposal**
*Audit of Technical Deliberations & Definitive Blueprint*

---

## 📋 **Summary**
This proposal integrates five previously orphaned governance modules (`OutputRouter`, `WorkflowEngine`, `GovernanceUnitOfWork`, etc.) into production services (`api.py`, `orchestrator.py`, `kanban_processor.py`) to enforce atomic writes, eliminate legacy fallback paths, and ensure council outputs land in the correct destination (`

---

## ⚠️ Difficulties & Constraints

| # | Challenge | Mitigation |
|---|---|---|
| 1 | **Atomicity in cross-filesystem writes** — UoW staging on a different filesystem from target breaks `os.rename()` guarantees | Enforce same-filesystem staging via `tempfile.mkdtemp(dir=<target_parent>)` |
| 2 | **Hard veto of `transition_rules` fallback** removes silent recovery path | Wrap `workflow_engine.transition()` in structured error handler that dead-letters AND alerts |
| 3 | **Legacy migration spans 7+ months** — data-loss risk if classifier is wrong | `--dry-run` mode emits JSON manifest of intended moves with SHA-256 checksums; human reviews before live run |
| 4 | **Feature flag misconfiguration** silently disables governance | Cache flags at startup with explicit warning + default to `true` per CSTR-PHASE5-V2 |

### Binding constraints (reaffirmed)

| Constraint | Description |
|---|---|
| CSTR-PHASE5-V1 | No new architecture; only wiring of existing modules |
| CSTR-PHASE5-V2 | Feature flags default to `true` |
| CSTR-PHASE5-V3 | Legacy paths must still work during migration |
| CSTR-PHASE5-V4 | All previous gates (35/35) stay green |
| CSTR-PHASE5-V5 | Real council verdicts land in `dev/decisions/` on first try |

### Hardening conditions from boardroom (binding, from v1.1)

| # | Condition |
|---|---|
| H1 | GovernanceUnitOfWork uses staging-area + undo log; same-filesystem staging only |
| H2 | Delete the `transition_rules` fallback dict; hard veto on WorkflowEngine failures |
| H3 | Direct OutputRouter injection into orchestrator.py (NOT via api.py delegation) |
| H4 | Migration script: `--dry-run` mode + JSON manifest + SHA-256 checksums |
| H5 | `smoke_phase5.py` runs in staging BEFORE production deploy |

---

## 🔧 Implementation Tasks

> Tick each item off as you complete it in VS Code.
> Update `tasks_completed` in the frontmatter as you go.
> **All paths relative to `cognitive-os/`** unless noted.

### Section A — Core wiring (5 services)

- [ ] **A1. `src/api.py` → OutputRouter**
   - [ ] At app startup: instantiate `OutputRouter(rules_path=paths.PROJECT_ROOT/"config"/"routing_rules.yaml", backend_writer=<inject>, dead_letter_dir=paths.PROJECT_ROOT/"dev"/"failed_routings")`
   - [ ] Replace inline string-matching in `/process` (the `if pattern == "boardroom"...` block) with `router.route(synthesis)` → `router.apply(...)`
   - [ ] Include the returned `RoutingDecision` in the `/process` JSON response
   - [ ] Boot-time validation: refuse to start if `load_routing_rules()` raises
   - [ ] Boot-time validation: refuse to start if `state_machine.yaml` is malformed
   - [ ] Boot-time: assert composite index exists on `approval_log(proposal_id, role, decision, ts)`

- [ ] **A2. `src/orchestrator.py` → OutputRouter (H3 — direct injection)**
   - [ ] Add `output_router: OutputRouter` field to `Orchestrator.__init__`
   - [ ] Orchestrator owns its router instance — do NOT route through api.py
   - [ ] Council-synthesis methods (`execute_sequential_boardroom`, `execute_orchestrated_meeting`, etc.) call `self.output_router.route(synthesis)` then `apply(...)`
   - [ ] Remove direct file I/O for synthesis output (no more `open(...).write(synthesis)`)
   - [ ] `_restore_default_state` keeps working (reads `simple.model` from master_config)

- [ ] **A3. `src/kanban_processor.py` → workflow_engine (H2 — hard cut)**
   - [ ] **DELETE** the `transition_rules` fallback dict in `_load_transition_rules`
   - [ ] In `_handle_card_transition`: wrap every phase write in `workflow_engine.transition(TransitionRequest(...))`
   - [ ] On WorkflowEngine veto: write the blocked transition to `dev/failed_routings/<id>_blocked.json` with full context (proposal_id, attempted phase, veto reason, timestamp)
   - [ ] Audit-log every block to `approval_log` table
   - [ ] **Keep** the existing `update_kanban_status` + `_set_proposal_processing_status` mirror-to-backend stopgap from `e34b022` until A4 lands

- [ ] **A4. `src/proposal_writer.py` + `src/handoff_writer.py` → GovernanceUnitOfWork (H1)**
   - [ ] Every multi-file write wraps in `with GovernanceUnitOfWork() as uow:`
   - [ ] UoW uses same-filesystem staging: `tempfile.mkdtemp(dir=<target_dir>.parent)`
   - [ ] UoW commit = atomic `os.rename(staged_path, final_path)` for each file
   - [ ] Undo log written to `dev/.uow_log/` before commit; deleted on success
   - [ ] On crash recovery: scan `dev/.uow_log/` at boot and roll back any incomplete transactions
   - [ ] Once UoW is live, the kanban_processor mirror-stopgap from `e34b022` can be removed in a follow-up commit

### Section B — Boot-time validation (H1, E3)

- [ ] **B1.** `dev/failed_routings/` exists at boot (create if missing — the `.gitkeep` is already in main)
- [ ] **B2.** `config/routing_rules.yaml` validates via `load_routing_rules()`
- [ ] **B3.** `config/state_machine.yaml` validates via `load_state_machine()` (or equivalent)
- [ ] **B4.** ApprovalLogger composite index exists; create if missing (Phase 1 already does this, just verify)

### Section C — Migration safety (H4, CSTR-PHASE5-V3)

- [ ] **C1.** Feature flags in `dev/master_config.md`:
  ```yaml
  integration:
    output_router_enabled: true
    workflow_engine_enabled: true
    governance_uow_enabled: true
  ```
  Each `false` reverts that subsystem to legacy behavior. Read once at startup; cache.

- [ ] **C2.** `scripts/migrate_ai_help_legacy.py` — one-shot migration script
   - [ ] Mode: `--dry-run` (default) reads each `AI-Help/cognitive-os/OLMR*.md`, classifies via OutputRouter, emits `migration_manifest.json` with: source_path, intended_destination, sha256, classification_confidence
   - [ ] Mode: `--apply` reads the manifest, moves files, leaves originals in `AI-Help/cognitive-os/_migrated/`
   - [ ] Refuses to run `--apply` if no manifest exists
   - [ ] Reports unclassifiable files (no marker match → catch-all) as a separate JSON section for human review

### Section D — Tests (≥10 integration cases)

- [ ] **D1. `tests/integration/test_api_process_uses_output_router.py`**
   - [ ] Fixture: synthesize a fake boardroom output containing `#boardroom`
   - [ ] Assert: lands in `dev/proposals/<test_id>_*.md`, NOT in `AI-Help/cognitive-os/`
   - [ ] Assert: response includes `routing_decision` with correct `rule_name` + `destination`

- [ ] **D2. `tests/integration/test_kanban_transition_uses_workflow_engine.py`**
   - [ ] Drag-simulation: write to kanban file, call `process_all_transitions`
   - [ ] Assert: `workflow_engine.transition()` is called
   - [ ] Assert: a vetoed transition produces a `dev/failed_routings/<id>_blocked.json`

- [ ] **D3. `tests/integration/test_orchestrator_no_legacy_fallback.py`**
   - [ ] Fire `execute_sequential_boardroom` on a fixture
   - [ ] Assert: `AI-Help/cognitive-os/` gains NO new files
   - [ ] Assert: synthesis lands in correct backend path per routing rules

- [ ] **D4. `tests/integration/test_proposal_writer_uses_uow.py`**
   - [ ] Patch one of the writes to raise mid-operation
   - [ ] Assert: NO files exist in target dirs (full rollback)
   - [ ] Assert: `dev/.uow_log/` is clean (undo log deleted)

- [ ] **D5. `tests/integration/test_uow_crash_recovery.py`**
   - [ ] Write a fake undo log to `dev/.uow_log/`
   - [ ] Boot UoW
   - [ ] Assert: incomplete transaction rolled back; undo log cleared

- [ ] **D6. `tests/integration/test_migration_dry_run.py`**
   - [ ] Fixture: 3 files in fake `AI-Help/cognitive-os/`
   - [ ] Run script with `--dry-run`
   - [ ] Assert: manifest.json contains 3 entries with correct sha256s; no files moved

- [ ] **D7-D10.** Additional cases covering: routing-rules malformed yaml refuses startup; feature flag off bypasses subsystem; concurrent transitions don't corrupt UoW log; unclassifiable file in migration goes to manual-review section

### Section E — Smoke test (H5)

- [ ] **E1. `scripts/smoke_phase5.py`**
   - [ ] Step 1: Fire a fake boardroom via the API (POST a synthesis fixture)
   - [ ] Step 2: Assert synthesis landed in `dev/decisions/` (NOT `AI-Help/cognitive-os/`)
   - [ ] Step 3: Assert the kanban card status updated
   - [ ] Step 4: Assert `approval_log` SQLite gained one entry
   - [ ] Exit code 0 = green; 1 + diagnostic JSON dump on failure
   - [ ] Document in proposal as the gate that must pass BEFORE production deploy (H5, CSTR-PHASE5-V5)

### Section F — Acceptance gate

- [ ] **F1.** Run `python scripts/alpha_polish_check.py --phase all` → must report 35/35 PASS (plus any new Phase 5 gates if added)
- [ ] **F2.** Run `python -m pytest tests/ -v` → all green, ≥10 new integration tests pass
- [ ] **F3.** Run `python scripts/smoke_phase5.py` → exit 0
- [ ] **F4.** Manual: drag a test card on the kanban; verify verdict lands in `dev/decisions/` first try (CSTR-PHASE5-V5)
- [ ] **F5.** Commit hygiene: one logical commit per section (A1, A2, A3, A4 each their own commit). NO megacommits.

---

## 🚦 Stop & report when done

When all F1-F5 are green:

1. Run all three gates one final time and paste the outputs into your final message
2. Run `git status` and `git log --oneline -10`
3. Say "**submitted for verification**" and stop
4. Do NOT self-attest "complete" — the human runs the truth check

---

## 🧱 Reading order for the implementing agent

1. The **proposal**: `dev/proposals/ARCH-20260523-235908-49798A0E_PROPOSAL.md` (especially v1.1 hardening conditions)
2. The **boardroom verdict**: `dev/decisions/ARCH-20260523-235908-49798A0E_boardroom_decision.md`
3. The **existing modules you wire UP** (already tested, do NOT modify):
   - `src/output_router.py`, `src/workflow_router.py`, `src/routing_rules_schema.py`
   - `src/workflow_engine.py`, `src/git_operations.py`, `src/workflow_models.py`
   - `src/handoff_vault.py`, `src/approval_logger.py`, `src/governance_unit_of_work.py`
4. The **services you wire INTO** (modify these):
   - `src/api.py`, `src/orchestrator.py`, `src/kanban_processor.py`
   - `src/proposal_writer.py`, `src/handoff_writer.py`
5. The **style + constraint docs**:
   - `docs/style/AESTHETIC.md`
   - `docs/CONSTRAINT_PREMATURE_SYNCHRONIZATION.md`



## 🧠 Technical Council Deliberation

<details>
<summary>Full council report (click to expand)</summary>

```markdown
# **ARCH-20260523-235908-49798A0E: Governance Stack Integration Proposal**
*Audit of Technical Deliberations & Definitive Blueprint*

---

## 📋 **Summary**
This proposal integrates five previously orphaned governance modules (`OutputRouter`, `WorkflowEngine`, `GovernanceUnitOfWork`, etc.) into production services (`api.py`, `orchestrator.py`, `kanban_processor.py`) to enforce atomic writes, eliminate legacy fallback paths, and ensure council outputs land in the correct destination (`dev/decisions/`) on first try. The core goal is to transition from a fragmented governance stack to a cohesive, end-to-end operational system.

**Key Achievements After Implementation:**
- `api.py` routes council synthesis via `OutputRouter`, eliminating string-matching fallbacks.
- `orchestrator.py` writes outputs through `OutputRouter` instead of direct file I/O.
- `kanban_processor.py` transitions phases via `workflow_engine.transition()`, with explicit dead-lettering for failures.
- `proposal_writer.py`/`handoff_writer.py` use `GovernanceUnitOfWork` for atomic multi-file commits.
- Legacy fallback paths (`AI-Help/cognitive-os/`) are preserved during migration but will be renamed to `_legacy/` post-migration.

---

## 🔴 **Difficulties & Constraints**
### **Technical Challenges**
1. **Atomicity in Cross-Filesystem Writes**
   - The `GovernanceUnitOfWork` staging area must ensure atomic commits even if the staging and target directories are on different filesystems.
   - *Mitigation:* Enforce same-filesystem staging (H1) or use a filesystem-agnostic atomic move pattern (e.g., `os.rename()` with error handling).

2. **Hard Vetoes in Workflow Transitions**
   - Removing the `transition_rules` fallback (H2) requires explicit dead-lettering/alerting for `WorkflowEngine` failures to prevent silent drops.
   - *Mitigation:* Wrap `workflow_engine.transition()` in structured error handling with logging to a dead-letter queue.

3. **Migration Integrity**
   - Legacy files span 7+ months; idempotency, checksums, and conflict resolution are critical to avoid data loss (CSTR-PHASE5-V3).
   - *Mitigation:* Implement `--dry-run` mode in `migrate_ai_help_legacy.py` with SHA-256 checksum verification.

4. **Feature Flag Validation**
   - Robust validation of feature flags in `master_config.md` to avoid silent misconfigurations.
   - *Mitigation:* Cache flags in memory, log warnings for invalid values, and ensure defaults align with CSTR-PHASE5-V2.

### **Binding Constraints (Reaffirmed)**
| Constraint | Description |
|------------|-------------|
| **CSTR-PHASE5-V1** | No new architecture; only wiring of existing modules. |
| **CSTR-PHASE5-V2** | Feature flags default to `true`; reverting requires explicit rollback steps. |
| **CSTR-PHASE5-V3** | Legacy paths must still work during migration (no data loss). |
| **CSTR-PHASE5-V4** | All previous gates stay green (`python scripts/alpha_polish_check.py`). |
| **CSTR-PHASE5-V5** | Real council verdicts land in `dev/decisions/` on first try. |

---

## 🚀 **Implementation Tasks**
### **Core Integration (5 Services)**
1. **`api.py` → `OutputRouter`**
   - Replace inline string-matching with an injected `OutputRouter`.
   - Validate `config/routing_rules.yaml` and `config/state_machine.yaml` on startup.
   - Include `RoutingDecision` in API responses.

2. **`orchestrator.py` → `OutputRouter`**
   - Inject `OutputRouter` directly into orchestrator’s constructor (H3).
   - Return council synthesis to `api.py` for routing.

3. **`kanban_processor.py` → `WorkflowEngine`**
   - Replace `transition_rules` fallback with explicit dead-lettering.
   - Log transitions blocked by `workflow_engine.transition()` failures.

4. **`proposal_writer.py`/`handoff_writer.py` → `GovernanceUnitOfWork`**
   - Wrap multi-file writes in `with GovernanceUnitOfWork() as uow:`.
   - Implement staging-area + undo log for crash recovery (H1).

5. **Feature Flags & Migration Scripts**
   - Add flags to `master_config.md`:
     ```yaml
     integration:
       output_router_enabled: true
       workflow_engine_enabled: true
       governance_uow_enabled: true
     ```
   - Develop `scripts/migrate_ai_help_legacy.py` with `--dry-run`, JSON manifest, and checksums.

### **Validation & Testing**
6. **Integration Tests** (`tests/integration/`)
   - Test API routing, kanban transitions, orchestrator dead-lettering.
   - Target ≥10 cases (e.g., crash simulations, workflow vetoes).

7. **Smoke Test** (`scripts/smoke_phase5.py`)
   - Simulate a boardroom verdict; assert it lands in `dev/decisions/`.
   - Fail pipeline if CSTR-PHASE5-V5 fails.

### **Hardening Conditions (Binding)**
8. **GovernanceUnitOfWork Staging**
   - Use same-filesystem staging + atomic `os.rename()`.
   - Implement undo log for crash recovery.

9. **WorkflowEngine Hard Cut**
   - Delete `transition_rules` fallback; block transitions on vetoes.

10. **Migration Safety**
    - `--dry-run` mode with JSON manifest and SHA-256 checksums.

---

## 🔬 **Technical Recommendations**
### Architecture
- **Staging-to-Final Atomicity**: Use `os.rename()` for same-filesystem UoW commits.
- **Exception Hierarchy**:
  ```python
  class GovernanceError(Exception): ...
  class RoutingError(GovernanceError): ...
  class TransitionVetoed(RoutingError): ...
  class UoWIntegrityError(GovernanceError): ...
  ```

### Libraries & Patterns
| Component | Recommendation |
|-----------|----------------|
| **GovernanceUnitOfWork** | Same-filesystem staging + undo log. |
| **OutputRouter** | Direct injection into orchestrator (H3). |
| **WorkflowEngine** | Dead-lettering for vetoed transitions. |
| **Migration Script** | `--dry-run` + JSON manifest + checksums. |

### Testing Strategy
- **Crash Simulation**: Kill processes mid-UoW to test recovery.
- **Routing Misconfigurations**: Test invalid `routing_rules.yaml`.
- **CI/CD Gate**: Smoke test in staging before production.

---

## 📜 **Definitive Verdict & Outcome**
**Status**: Approved with Hardening Conditions
**Key Decisions**:
1. **Rejected "Dark Mirror" Git Strategy**: Violates CSTR-PHASE5-V1 and H1’s staging-area requirement.
2. **Enforced Same-FileSystem Staging**: Atomic `os.rename()` for UoW atomicity.
3. **Hard Cut on Fallbacks**: Removed `transition_rules`; dead-lettering required (H2).
4. **Feature Flags Default ON**: No silent regressions during migration.

**Rollback Plan**:
```yaml
1. Set feature flags to false.
2. Git revert wiring commits.
3. Replay dead-letters via restore script.
```

**Post-Deploy Follow-Ups**:
- Rename `AI-Help/cognitive-os/` → `_legacy/`.
- Add Telegram alerts for `dev/failed_routings/`.
- 30-day audit to remove feature flags if all-green.

---
```markdown
# **ARCH-20260523-235908-49798A0E: Governance Stack Integration Proposal**
**Phase**: Backlog → Approved with Hardening Conditions
**Status**: ✅ *Finalized*

---

## 📋 **Summary**
This proposal integrates five orphaned governance modules (`OutputRouter`, `WorkflowEngine`, `GovernanceUnitOfWork`) into production services (`api.py`, `orchestrator.py`, `kanban_processor.py`). The goal is to eliminate legacy fallback paths, enforce atomic multi-file writes, and ensure council outputs land in the correct destination (`dev/decisions/`) on first try.

**Key Outcomes**:
- **No new architecture**: All modules are wired from existing tested code.
- **Atomicity**: `GovernanceUnitOfWork` uses same-filesystem staging + undo log for crash recovery.
- **Hard Vetoes**: WorkflowEngine failures trigger dead-lettering/alerts (no silent drops).
- **Migration Safety**: Legacy data is migrated via `--dry-run` scripts with checksum verification.

---

## 🔴 **Difficulties & Constraints**
### **Technical Challenges**
| Challenge | Mitigation Strategy |
|-----------|---------------------|
| Atomicity risk in UoW | Same-filesystem staging + `os.rename()` for atomic commits. |
| Cross-filesystem moves | Rejected; enforces filesystem-local staging (H1). |
| Feature flag validation | Robust parsing with defaults and explicit warnings. |
| Migration integrity | `--dry-run` mode, JSON manifests, SHA-256 checksums. |

### **Binding Constraints**
1. **No New Architecture** (`CSTR-PHASE5-V1`): All changes are wiring-only.
2. **Feature Flags Default ON** (`CSTR-PHASE5-V2`): No silent regressions.
3. **Legacy Paths Must Work** (`CSTR-PHASE5-V3`): Migration script preserves data until post-migration.
4. **All Gates Stay Green** (`CSTR-PHASE5-V4`): No regressions in existing tests.
5. **First-Try Success** (`CSTR-PHASE5-V5`): Real council verdicts land in `dev/decisions/`.

### **Vetoed Approaches**
- **"Dark Mirror" Git Strategy**: Violates CSTR-PHASE5-V1 and H1’s staging-area requirement.
- Cross-filesystem staging for UoW: Breaks atomic rename guarantees.

---

## 📋 **Implementation Tasks**
### **Core Integration (5 Services)**
1. **`api.py /process` → `OutputRouter`**:
   - Replace inline string-matching with `OutputRouter.route(synthesis)`.
   - Include `RoutingDecision` in API response.
2. **`orchestrator.py` → `OutputRouter`**:
   - Inject `OutputRouter` directly into orchestrator (no delegation).
3. **`kanban_processor.py` → `workflow_engine`**:
   - Replace `transition_rules` fallback with `workflow_engine.transition()`.
   - Log hard vetoes to dead-letter/alerts.
4. **`proposal_writer.py` + `handoff_writer.py` → `GovernanceUnitOfWork`**:
   - Wrap multi-file writes in `with GovernanceUnitOfWork() as uow:`.
5. **Boot-time Validation** (`api.py`):
   - Validate `routing_rules.yaml`, `state_machine.yaml`, and SQLite composite index.

### **Migration & Safety**
6. **Feature Flags** (`master_config.md`):
   ```yaml
   integration:
     output_router_enabled: true
     workflow_engine_enabled: true
     governance_uow_enabled: true
   ```
7. **Legacy Migration Script**:
   - `scripts/migrate_ai_help_legacy.py`: `--dry-run`, JSON manifest, checksum verification.
8. **Integration Tests** (`tests/integration/`):
   - ≥10 test cases covering crash-simulation, routing failures, and migration idempotency.

### **Post-Deploy Follow-Ups**
9. **Smoke Test** (`scripts/smoke_phase5.py`):
   - Validates CSTR-PHASE5-V5 (verdict lands in `dev/decisions/`).
10. **Dashboard Alerts**:
    - Monitor `dev/failed_routings/` for new entries via Telegram.

---

## 🛠️ **Technical Recommendations**
### **Architecture & Patterns**
- **Atomicity**: Use same-filesystem staging + `os.rename()` for `GovernanceUnitOfWork`.
  ```python
  def atomic_write(self, files):
      temp_dir = self._get_temp_dir()
      try:
          # Write to temp_dir first
          os.makedirs(temp_dir, exist_ok=True)
          for file in files:
              shutil.copy(file, os.path.join(temp_dir, os.path.basename(file)))
          # Atomic rename on success
          os.rename(temp_dir, self._target_dir)
      except Exception as e:
          # Rollback: delete temp_dir and undo log
          raise UoWIntegrityError("Atomic write failed") from e
  ```
- **Exception Hierarchy**:
  ```python
  class GovernanceError(Exception): pass
  class RoutingError(GovernanceError): pass
  class TransitionVetoed(RoutingError): pass
  class UoWIntegrityError(GovernanceError): pass
  ```

### **Libraries & Tools**
- **Git Operations**: Leverage `git_operations.py` for shadow commit patterns (if feasible).
- **Validation**: Use `Pydantic` or `jsonschema` for config parsing.
- **Testing**: Mock filesystem operations and DB locks in integration tests.

### **Hardening Conditions (Binding)**
| Condition | Implementation |
|-----------|----------------|
| **H1** (Staging + Undo Log) | Same-filesystem staging with atomic rename. |
| **H2** (No Fallback Dicts) | Delete `transition_rules`; log hard vetoes. |
| **H3** (Direct Router Injection) | Inject `OutputRouter` into orchestrator via constructor. |
| **H4** (Dry-Run Migration) | `--dry-run` flag + JSON manifest in migration script. |
| **H5** (Smoke Test CI Gate) | Run `smoke_phase5.py` post-deploy; fail pipeline on failure. |

### **Risk Mitigation**
- **Data Loss**: Use checksums and idempotent migration scripts.
- **Silent Failures**: Dead-letter alerts for WorkflowEngine vetoes.
- **Test Coverage**: Simulate crashes, DB locks, and routing misconfigurations.

---
## 📜 **Final Deliberation Summary**
### **Core Outcome**
The proposal successfully integrates five orphaned governance modules into production services while adhering to strict constraints. The key decisions were:
1. **Reject the 'Dark Mirror' git commit strategy** (violates CSTR-PHASE5-V1).
2. **Enforce same-filesystem staging for `GovernanceUnitOfWork`** (mitigates atomicity risk).
3. **Harden WorkflowEngine failures** with dead-letter/alerting (no silent fallbacks).

### **Definitive Blueprint**
```markdown
# ARCH-20260523-235908-49798A0E: Governance Integration Proposal

## 📋 Summary
This proposal wires five orphaned governance modules (`OutputRouter`, `WorkflowEngine`, `GovernanceUnitOfWork`) into production services (`api.py`, `orchestrator.py`, `kanban_processor.py`), ensuring atomic writes, routed outputs, and migration safety. The goal is to eliminate legacy fallback paths and enforce end-to-end governance.

## 🔴 Difficulties & Constraints
- **Atomicity**: Cross-filesystem staging risks partial writes; use same-filesystem `os.rename()` with undo logs.
- **Hard Vetoes**: WorkflowEngine failures must trigger dead-letter/alerts (no silent drops).
- **Migration Safety**: Legacy files require idempotent, checksum-verified migration scripts.
- **Feature Flags**: Default to `ON`; revert if production reveals bugs.

## 📋 Implementation Tasks
1. Implement `GovernanceUnitOfWork` with same-filesystem staging and undo log.
2. Delete `transition_rules` fallback in `kanban_processor.py`; wrap `workflow_engine.transition()` in error handling.
3. Inject `OutputRouter` directly into `orchestrator.py` (decouple from `api.py`).
4. Develop `scripts/migrate_ai_help_legacy.py` with `--dry-run` and JSON manifest.
5. Add feature flags to `master_config.md` (default `TRUE`).
6. Write integration tests covering crashes, workflow vetoes, and migration idempotency.

## 🛠 Technical Recommendations
- **Architecture**: Use atomic `os.rename()` for staging-to-final commits.
- **Libraries**: Pydantic for config validation; mock filesystem/DB in tests.
- **Patterns**:
  - Exception hierarchy: `RoutingError`, `TransitionVetoed`, `UoWIntegrityError`.
  - Feature flags with strict defaults and runtime warnings.

## 🚨 Binding Constraints (Reaffirmed)
| Constraint | Status |
|------------|--------|
| No new architecture | ✅ Enforced |
| Feature flags default ON | ✅ Enforced |
| Legacy paths still work | ✅ Enforced |
| All gates stay green | ✅ Enforced |
| Real council lands in `dev/decisions/` | ✅ Verified |

## 📝 Next Steps
1. Finalize same-filesystem staging layout for `GovernanceUnitOfWork`.
2. Integrate smoke test (`smoke_phase5.py`) into CI/CD.
3. Deploy during low-traffic periods; monitor dead-letter alerts.
4. Post-deploy audit: rename `AI-Help/cognitive-os/` to `_legacy/`.

---
**Veto Points Resolved**:
✅ Cross-filesystem staging rejected (violated H1).
✅ Transition_rules fallback deleted (H2 enforced).
✅ Dead-lettering/alerts implemented for WorkflowEngine failures.
```

---
*Final Verdict: The proposal is **approved with hardening conditions** and will proceed to implementation. All constraints are satisfied, and the integration is designed for zero regressions.*

</details>

---

## 📝 Developer Notes

> *Fill in as you work through the tasks above.*

<!-- Add your implementation notes, decisions, and blockers here -->

---

## ✅ Completion Gate

Before moving the Kanban card to **Alpha Polish**, confirm:

- [ ] All implementation tasks above are ticked
- [ ] Core functionality works end-to-end
- [ ] No critical bugs remain
- [ ] Basic manual testing passed
- [ ] Frontmatter `status` updated to `complete`
- [ ] Proposal `## 🧪 Beta Testing` section updated to `✅ Complete`

---

*Handoff generated by Cognitive OS Technical Council*  
*Card stays in **Beta Testing** until all tasks above are complete.*  
*Only then move the card to **Alpha Polish**.*
