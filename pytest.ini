[pytest]
markers =
    asyncio: Test uses async/await patterns
    manual: Live tests that require human intent (real LLM calls, real I/O,
            long runtime). Skipped by default. Opt-in via env vars documented
            on the test itself, e.g. RUN_LIVE_COUNCIL=1.
testpaths = tests
asyncio_default_fixture = asyncio
